using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Swap2 : MonoBehaviour
{
    /*
     * swap2
click on card to swap (yours or opponents) 
determine which player/array is being manipulated
determine which adjacent card is also to be swapped
limit second click to array other than that player's holder
select second card
determine which adjacent card is also to be swapped
perform a standard 3-step move for swapping values in two memory variables for first card
perform a standard 3-step move for swapping values in two memory variables for second card

*/

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
