using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Flip : MonoBehaviour
{
    /*
     * click on card to flip (yours or opponents) 
determine which player/array is being manipulated
update display
confirm choice
change visible from A -> B or B -> A
switch
click on card to switch (yours or opponents) 
determine which player/array is being manipulated
limit second click to that player's holder
select second card
perform a standard 3-step move for changing places in an array
*/

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
