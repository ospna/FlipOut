using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Swap1 : MonoBehaviour
{
    /*
     * swap1
click on card to swap (yours or opponents) 
determine which player/array is being manipulated
limit second click to array other than that player's holder
select second card
perform a standard 3-step move for swapping values in two memory variables 

*/

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
